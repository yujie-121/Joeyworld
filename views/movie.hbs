<h1>Movie</h1>
{{#if movie}}
  <p>The movie {{movie.title}} get grade {{movie.grade}}/10.</p>
  <a href="/update-movie/{{movie.id}}">Update</a>
  <form action="/delete-movie/{{movie.id}}" method="post">
    <input type="submit" value="Delete" />
  </form>
{{else}}
  <p>That movie doesn't exist.</p>
{{/if}}